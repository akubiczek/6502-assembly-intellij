# Gradle 9 compatibility settings
org.gradle.jvmargs=-Xmx2g -XX:+HeapDumpOnOutOfMemoryError -XX:+UseParallelGC -Dfile.encoding=UTF-8

# Enable configuration cache (Gradle 6.6+)
org.gradle.configuration-cache=true

# Enable build cache (Gradle 3.5+)
org.gradle.caching=true

# Enable parallel execution
org.gradle.parallel=true

# Kotlin settings for build scripts (if needed)
kotlin.code.style=official